using System;

namespace MPP_lab4
{
    class Program
    {
        static void Main(string[] args)
        {
            int handle = 0;
            var osHandle = new OSHandle(new IntPtr(handle));//создаю экземпялр
        }
    }
}